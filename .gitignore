# Runtime state — never committed
/data/
/artifacts/
/worktrees/

# Isolated codex config home for grid runs (contains symlinked auth)
/.codex-grid-home/

# Node
node_modules/
npm-debug.log*

# OS
.DS_Store

# Keep the example target repo's own .git out of this repo (it's a nested demo repo)
/examples/target-repo/
